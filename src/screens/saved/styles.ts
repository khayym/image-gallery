import {StyleSheet} from 'react-native';
import {pixelHorizontal} from 'theme/metrics';

export const styles = StyleSheet.create({
  content: {
    paddingBottom: 40,
  },
  root: {
    paddingHorizontal: pixelHorizontal(16),
    flex: 1,
  },
});
